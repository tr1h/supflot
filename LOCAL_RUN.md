# 🚀 Локальный запуск SUPFLOT

## ✅ Проверка структуры проекта

Проект полностью проверен и готов к запуску!

### 📋 Что проверено:

1. ✅ **Конфигурация** - `.env` файл создан с токеном
2. ✅ **Зависимости** - все установлены из `requirements.txt`
3. ✅ **Структура файлов** - все на месте
4. ✅ **Handlers** - все обработчики зарегистрированы
5. ✅ **База данных** - схема готова, создастся автоматически

## 🎯 Запуск бота

### Вариант 1: Через bat-файл (Windows)

Двойной клик на `START_BOT.bat`

### Вариант 2: Через командную строку

```bash
python run_bot.py
```

### Что произойдет:

1. ✅ Загрузится конфигурация из `.env`
2. ✅ Создастся база данных `supbot.db` (если нет)
3. ✅ Загрузятся тестовые данные (партнеры, локации, доски)
4. ✅ Бот начнет отвечать на команды

### Команды бота:

- `/start` - Начать работу
- `/help` - Помощь
- `/daily` - Суточная аренда
- `/partner` - Партнерская панель
- `/admin` - Админ-панель
- `/contacts` - Контакты
- `/offer` - Оферта

## 🌐 Запуск сайта

### Вариант 1: Через bat-файл (Windows)

Двойной клик на `START_WEBSITE.bat`

### Вариант 2: Через командную строку

```bash
cd orders_site
python app.py
```

### Что произойдет:

1. ✅ Запустится Flask сервер
2. ✅ Сайт будет доступен на http://localhost:5000
3. ✅ Создастся база данных `SupBot.db` (если нет)

### Доступные страницы:

- `http://localhost:5000/` - Главная страница
- `http://localhost:5000/locations` - API локаций
- `http://localhost:5000/boards/<id>` - API досок
- `http://localhost:5000/bookings` - API бронирований

## 📊 Структура проекта

### Основные компоненты:

1. **Бот** (`run_bot.py`)
   - Пользовательские команды
   - Партнерская система
   - Админ-панель
   - Платежи
   - Кошелек партнеров

2. **Сайт** (`orders_site/app.py`)
   - Онлайн-бронирование
   - API для фронтенда
   - Интеграция с ЮKassa

3. **База данных**
   - SQLite (локально)
   - Автоматическое создание таблиц
   - Тестовые данные при первом запуске

## 🔍 Проверка работы

### Бот:

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Должен прийти ответ с меню

### Сайт:

1. Откройте браузер
2. Перейдите на http://localhost:5000
3. Должна открыться главная страница

## ⚠️ Важно

1. **Бот и сайт используют разные БД:**
   - Бот: `supbot.db` (в корне)
   - Сайт: `SupBot.db` (в orders_site/)
   
   В будущем нужно объединить!

2. **Тестовые данные:**
   - При первом запуске бота загрузятся тестовые данные
   - 3 партнера, локации, доски, бронирования
   - Можно использовать для тестирования

3. **Логи:**
   - Логи бота: `bot.log`
   - Логи сайта: в консоли

## 🐛 Решение проблем

### Бот не запускается:

1. Проверьте `.env` файл
2. Проверьте токен бота
3. Проверьте логи в консоли
4. Убедитесь, что все зависимости установлены

### Сайт не открывается:

1. Проверьте, что порт 5000 свободен
2. Проверьте логи в консоли
3. Убедитесь, что Flask установлен

### Ошибки импорта:

Запустите проверку:
```bash
python check_imports.py
```

## 📝 Следующие шаги

1. ✅ Запустите бота
2. ✅ Протестируйте команды
3. ✅ Запустите сайт
4. ✅ Проверьте бронирование
5. 🚀 Готово к деплою на Render!

