# 📋 Документация: Сервис напоминаний о бронированиях

## 📖 Обзор

Модуль `reminder_service.py` предоставляет функционал для отправки автоматических напоминаний пользователям о предстоящих бронированиях SUP-досок.

## 🎯 Основные функции

### `send_booking_reminders(bot, db) -> List[int]`

Основная функция для массовой отправки напоминаний о предстоящих бронированиях.

**Параметры:**
- `bot` (Bot): Экземпляр Telegram бота (aiogram)
- `db` (Database): Экземпляр базы данных

**Возвращает:**
- `List[int]`: Список ID пользователей, которым успешно отправлены напоминания

**Описание:**
Функция автоматически находит все активные бронирования, которые начинаются через 1 час от текущего времени, и отправляет напоминания пользователям. Работает только с бронированиями на текущую дату.

**Пример использования:**
```python
from notifications.reminder_service import send_booking_reminders
from core.database import Database
from aiogram import Bot

bot = Bot(token="YOUR_TOKEN")
db = Database()
await db.connect()

sent_to = await send_booking_reminders(bot, db)
print(f"Напоминания отправлены {len(sent_to)} пользователям")
```

**Логика работы:**
1. Вычисляет время через 1 час от текущего момента
2. Ищет активные бронирования на сегодня с временем начала, совпадающим с вычисленным временем
3. Для каждого найденного бронирования формирует и отправляет сообщение пользователю
4. Логирует успешные отправки и ошибки
5. Возвращает список ID пользователей, которым отправлены напоминания

**Обработка ошибок:**
- Ошибки при отправке конкретному пользователю логируются, но не прерывают обработку остальных
- Все исключения перехватываются и логируются
- При ошибке функция возвращает пустой список или частичный список успешных отправок

---

### `send_booking_reminder_for_user(bot, db, user_id, booking_id) -> bool`

Отправка напоминания конкретному пользователю о конкретном бронировании.

**Параметры:**
- `bot` (Bot): Экземпляр Telegram бота
- `db` (Database): Экземпляр базы данных
- `user_id` (int): ID пользователя
- `booking_id` (int): ID бронирования

**Возвращает:**
- `bool`: `True` если напоминание отправлено успешно, `False` в противном случае

**Описание:**
Проверяет существование и активность указанного бронирования для пользователя, затем отправляет напоминание. Используется для ручной отправки напоминаний или в специальных случаях.

**Пример использования:**
```python
success = await send_booking_reminder_for_user(bot, db, user_id=123, booking_id=456)
if success:
    print("Напоминание отправлено")
else:
    print("Не удалось отправить напоминание")
```

**Условия отправки:**
- Бронирование должно существовать
- Бронирование должно принадлежать указанному пользователю
- Статус бронирования должен быть `'active'`

---

## 🔄 Интеграция с планировщиком

Функция `send_booking_reminders()` интегрирована в `NotificationScheduler` и вызывается автоматически каждую минуту.

**Расположение:** `notifications/notification_scheduler.py`

```python
async def _check_and_send_reminders(self):
    """Проверка и отправка напоминаний о предстоящих бронированиях"""
    from notifications.reminder_service import send_booking_reminders
    sent = await send_booking_reminders(self.bot, self.db)
    if sent:
        logger.info(f"Sent booking reminders to {len(sent)} users")
```

## 📝 Формат сообщения

Напоминание отправляется в следующем формате:

```
⏰ Напоминание!

Ваше бронирование начинается через час:

📅 Дата: 2024-12-31
⏰ Время: 15:00
🏄 Доска: SUP доска Classic
⏱ Длительность: 2 час(ов)
📊 Количество: 2
```

## ⚙️ Настройки

### Временной интервал

Напоминания отправляются **за 1 час** до начала бронирования. Это значение захардкожено в функции.

Для изменения интервала нужно модифицировать:
```python
reminder_time = now + timedelta(hours=1)  # Измените hours на нужное значение
```

### Частота проверки

Планировщик проверяет напоминания **каждую минуту** (60 секунд). Это значение настраивается в `NotificationScheduler.start()`:

```python
await asyncio.sleep(60)  # Измените на нужное значение
```

## 🐛 Логирование

Все действия логируются через стандартный модуль `logging`:

- **DEBUG**: Когда нет бронирований для напоминаний
- **INFO**: Успешная отправка напоминаний (количество пользователей)
- **ERROR**: Ошибки при отправке конкретным пользователям или общие ошибки

**Пример логов:**
```
INFO: Reminder sent to user 123 for booking 456
INFO: Sent 5 booking reminders
ERROR: Error sending reminder to user 789: Bot blocked by user
```

## 🧪 Тестирование

Для тестирования функции можно:

1. **Создать тестовое бронирование** на сегодня с временем начала через час
2. **Запустить функцию вручную:**
   ```python
   await send_booking_reminders(bot, db)
   ```
3. **Проверить логи** и сообщения в Telegram

## 📌 Примечания

- Функция работает только с бронированиями на **текущую дату**
- Учитываются только бронирования со статусом **`'active'`**
- Время проверяется с точностью до **минуты**
- Если пользователь заблокировал бота, ошибка логируется, но не прерывает работу

## 🔮 Возможные улучшения

- [ ] Настраиваемый интервал напоминаний (например, через конфигурацию)
- [ ] Напоминания за разные интервалы (1 час, 30 минут, 15 минут)
- [ ] Поддержка напоминаний для бронирований на следующие дни
- [ ] Статистика отправленных напоминаний
- [ ] Настройки пользователя (включить/выключить напоминания)

