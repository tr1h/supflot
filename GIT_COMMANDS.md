# 🚀 Команды для загрузки проекта на GitHub

## ⚠️ ВАЖНО: Сначала создайте репозиторий на GitHub!

1. Перейдите на https://github.com/new
2. Название: `supflot`
3. **НЕ** добавляйте README, .gitignore, лицензию
4. Нажмите "Create repository"

## 📋 Последовательность команд

```powershell
# 1. Инициализация (уже сделано)
git init

# 2. Добавить все файлы
git add .

# 3. Создать первый коммит
git commit -m "Initial commit: SUPFLOT platform"

# 4. Переименовать ветку в main (если нужно)
git branch -M main

# 5. Добавить remote (после создания репозитория на GitHub)
git remote add origin https://github.com/tr1h/supflot.git

# 6. Отправить код на GitHub
git push -u origin main
```

## 🔍 Проверка перед push

```powershell
# Проверить статус
git status

# Проверить, что будет закоммичено
git diff --cached

# Проверить remote
git remote -v
```

## ❌ Если репозиторий уже существует на GitHub

Если вы случайно создали репозиторий с README, выполните:

```powershell
# Получить изменения с GitHub
git pull origin main --allow-unrelated-histories

# Разрешить конфликты (если есть)
# Затем:
git push -u origin main
```

## ✅ После успешного push

Проверьте на GitHub:
- https://github.com/tr1h/supflot

Все файлы должны быть видны!

