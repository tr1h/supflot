# 🚀 Telegram Mini App для SUPFLOT

## ✅ Что создано

Создано красивое Telegram Mini App - веб-приложение, которое открывается прямо в Telegram!

### 📱 Файлы:

1. **`orders_site/miniapp.py`** - Flask Blueprint для Mini App
2. **`orders_site/templates/miniapp/index.html`** - Красивый интерфейс
3. **Интеграция в бот** - кнопка "🚀 Открыть приложение"

## 🎨 Особенности дизайна

- ✅ **Адаптивный дизайн** - работает на всех устройствах
- ✅ **Telegram WebApp API** - интеграция с Telegram
- ✅ **Темная/светлая тема** - автоматически подстраивается под тему Telegram
- ✅ **Современный UI** - градиенты, анимации, плавные переходы
- ✅ **Haptic Feedback** - тактильная обратная связь
- ✅ **Быстрые действия** - моментальная, предварительная, суточная аренда

## 🚀 Функционал Mini App

1. **Главный экран:**
   - Hero секция с анимацией
   - Быстрые действия (4 карточки)
   - Список локаций

2. **Бронирование:**
   - Выбор локации
   - Выбор доски
   - Дата и время
   - Продолжительность

3. **API:**
   - `/miniapp/api/locations` - список локаций
   - `/miniapp/api/boards/<id>` - доски по локации
   - `/miniapp/api/bookings` - создание бронирования

## 📋 Настройка

### 1. Локально

1. Запустите сайт:
   ```bash
   cd orders_site
   python app.py
   ```

2. Используйте ngrok для туннеля:
   ```bash
   ngrok http 5000
   ```

3. Получите URL вида: `https://xxxx.ngrok.io`

4. Обновите `.env`:
   ```
   MINIAPP_URL=https://xxxx.ngrok.io/miniapp/
   ```

5. Перезапустите бота

### 2. На Render.com

1. После деплоя сайта получите URL: `https://supflot-website.onrender.com`

2. Обновите переменную окружения в Render:
   ```
   MINIAPP_URL=https://supflot-website.onrender.com/miniapp/
   ```

3. Или в `.env` локально (для бота):
   ```
   MINIAPP_URL=https://supflot-website.onrender.com/miniapp/
   ```

## 🎯 Использование

### В боте:

1. Отправьте `/start`
2. Нажмите кнопку **"🚀 Открыть приложение"**
3. Откроется красивое веб-приложение прямо в Telegram!

### Функции:

- **Моментально** - бронирование у воды сейчас
- **Предварительно** - выбор даты и времени
- **Суточная** - аренда на весь день
- **Мои брони** - история бронирований
- **Локации** - список всех локаций

## 🔧 Интеграция с ботом

Кнопка Web App добавлена в `keyboards/user.py`:

```python
kb.button(
    text="🚀 Открыть приложение",
    web_app=WebAppInfo(url=MINIAPP_URL)
)
```

## 📱 Telegram WebApp API

Mini App использует:
- `window.Telegram.WebApp` - основной API
- `tg.ready()` - инициализация
- `tg.expand()` - развернуть на весь экран
- `tg.themeParams` - тема Telegram
- `tg.HapticFeedback` - вибрация
- `tg.showAlert()` - уведомления

## 🎨 Дизайн

- **Цвета**: Градиент синий (#0088cc → #00b4ff)
- **Шрифты**: Системные (Apple, Android)
- **Анимации**: Плавные переходы, float эффект
- **Карточки**: Скругленные углы, тени
- **Кнопки**: Градиентные, с эффектом нажатия

## ✅ Готово!

Mini App полностью готов и интегрирован в бот!

Теперь пользователи могут:
- Открыть красивое приложение прямо в Telegram
- Бронировать сапборды через удобный интерфейс
- Видеть локации и доски
- Создавать бронирования

🎉 **Проект готов к использованию!**

