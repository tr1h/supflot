# 🌐 Настройка DNS для supflot.pro на Render.com

## 📋 Информация о домене

- **Домен**: `supflot.pro`
- **DNS серверы**: 
  - `ns1.hosting.reg.ru`
  - `ns2.hosting.reg.ru`
- **Регистратор**: reg.ru

## 🚀 Настройка DNS записей

### Шаг 1: Получите URL от Render

После создания Web Service на Render, вы получите URL вида:
- `https://supflot-website.onrender.com`

### Шаг 2: Настройка DNS в reg.ru

1. Войдите в панель управления reg.ru
2. Перейдите в раздел **"Домены"** → выберите `supflot.pro`
3. Откройте **"DNS-серверы и зона"**
4. Убедитесь, что используются DNS серверы reg.ru (уже настроено)

### Шаг 3: Добавьте DNS записи

В разделе **"Управление DNS-записями"** добавьте:

#### Для основного домена (supflot.pro):

**Вариант 1: CNAME запись (рекомендуется)**
```
Тип: CNAME
Имя: @
Значение: supflot-website.onrender.com
TTL: 3600
```

**Вариант 2: A запись (если CNAME не поддерживается)**
```
Тип: A
Имя: @
Значение: [IP адрес Render] (получите в настройках Render)
TTL: 3600
```

#### Для поддомена www (www.supflot.pro):

```
Тип: CNAME
Имя: www
Значение: supflot-website.onrender.com
TTL: 3600
```

### Шаг 4: Настройка в Render

1. В Render Dashboard откройте ваш Web Service
2. Перейдите в **"Settings"** → **"Custom Domains"**
3. Нажмите **"Add Custom Domain"**
4. Введите: `supflot.pro`
5. Render покажет инструкции по настройке DNS

## ⚠️ Важно для reg.ru

### Если CNAME для корневого домена (@) не поддерживается:

Reg.ru может не поддерживать CNAME для корневого домена. В этом случае:

1. **Используйте A запись:**
   - Получите IP адрес Render (в настройках Custom Domain)
   - Добавьте A запись с этим IP

2. **Или используйте поддомен:**
   - Настройте `www.supflot.pro` через CNAME
   - Настройте редирект с `supflot.pro` на `www.supflot.pro`

## 📝 Пример настроек DNS в reg.ru

```
Тип    | Имя | Значение                          | TTL
-------|-----|-----------------------------------|-----
A      | @   | [IP от Render]                   | 3600
CNAME  | www | supflot-website.onrender.com     | 3600
```

## ⏱️ Время активации

- DNS записи обновляются: **5-30 минут**
- SSL сертификат от Render: **автоматически, 5-10 минут**

## ✅ Проверка

После настройки проверьте:

1. **DNS записи:**
   ```bash
   nslookup supflot.pro
   # или
   dig supflot.pro
   ```

2. **Доступность сайта:**
   - Откройте `https://supflot.pro`
   - Должен открыться ваш сайт

3. **SSL сертификат:**
   - Проверьте, что есть зеленый замочек 🔒
   - Сертификат установится автоматически

## 🔧 Альтернативный вариант (через Cloudflare)

Если нужен больший контроль:

1. Перенесите DNS на Cloudflare (бесплатно)
2. Cloudflare поддерживает CNAME для корневого домена
3. Настройте проксирование через Cloudflare
4. Получите дополнительные функции (кэширование, защита)

## 📞 Поддержка

Если возникнут проблемы:
- **reg.ru**: поддержка через панель управления
- **Render**: support@render.com или документация

