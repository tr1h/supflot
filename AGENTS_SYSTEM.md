# 🤖 Система агентов OpenCode

Система агентов для автоматизации разработки через OpenCode, аналогичная подходу Zukutoke.

## 🎯 Концепция

Как у Zukutoke:
- **Оркестратор** управляет всеми задачами
- **Специализированные агенты** для различных типов работ:
  - Планирование
  - Разработка
  - Документация
  - Тестирование
- Работа через **OpenCode CLI**

## 📁 Структура

```
agents/
├── __init__.py              # Экспорты
├── base_agent.py           # Базовый класс агента
├── orchestrator.py         # Оркестратор
├── planning_agent.py       # Агент планирования
├── development_agent.py    # Агент разработки
├── documentation_agent.py  # Агент документации
├── testing_agent.py        # Агент тестирования
├── cli.py                  # CLI интерфейс
├── README.md               # Документация
└── example_pipeline.json   # Пример пайплайна
```

## 🚀 Быстрый старт

### 1. Проверка OpenCode

```bash
wsl bash -c "~/.opencode/bin/opencode --version"
```

### 2. Включение AI в .env

```env
AI_ENABLED=true
```

### 3. Использование через CLI

```bash
# Выполнение задачи
python -m agents.cli execute -t "Реализуй функцию для парсинга дат"

# Планирование
python -m agents.cli plan -g "Добавить систему отзывов"

# Код-ревью
python -m agents.cli review --file handlers/user_handlers.py

# Информация об агентах
python -m agents.cli agents
```

## 📖 Подробная документация

См. [agents/README.md](agents/README.md) для полной документации.

## 🔄 Отличия от подхода Zukutoke

**У Zukutoke:**
- OpenCode + API от GLM-4.7
- Codex и Claude Code в CLI на своих тарифах
- Один оркестратор управляет всеми задачами

**В нашей реализации:**
- OpenCode через CLI (как у Zukutoke)
- Все агенты через OpenCode
- Легко расширяемая система
- CLI интерфейс для удобства

## 💡 Примеры использования

### Планирование новой функции

```bash
python -m agents.cli execute \
  -t "Создай план для системы отзывов: пользователи оставляют отзывы после бронирования" \
  --type planning
```

### Реализация

```bash
python -m agents.cli execute \
  -t "Реализуй функцию create_review(user_id, booking_id, rating, comment)" \
  --type development
```

### Полный цикл (планирование + выполнение)

```bash
python -m agents.cli plan -g "Добавить систему отзывов в бот"
```

## 🎨 Интеграция с Cursor

Система работает как **дополнение к Cursor**:
- Cursor для интерактивной разработки
- Агенты для автоматизации задач
- Оркестратор для сложных пайплайнов

## 📝 Следующие шаги

1. Попробуйте выполнить простую задачу через CLI
2. Создайте свой пайплайн для сложной задачи
3. Интегрируйте агентов в ваш workflow разработки

