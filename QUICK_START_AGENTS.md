# ⚡ Быстрый старт с агентами - за 2 минуты

## 🎯 Что вам нужно знать

**OpenCode - это инструмент, который работает автоматически.**
**Наши агенты - это команды, которые вы запускаете в терминале.**

Всё просто! 👇

---

## 📝 3 команды для начала работы

### 1. Показать агентов
```bash
python -m agents.cli agents
```
Покажет список доступных агентов.

### 2. Выполнить задачу
```bash
python -m agents.cli execute -t "Ваша задача здесь"
```

### 3. Планирование
```bash
python -m agents.cli plan -g "Ваша цель здесь"
```

---

## 🎨 Примеры из реальной жизни

### Пример 1: Нужна функция
```bash
python -m agents.cli execute -t "Реализуй функцию для отправки email уведомлений"
```

### Пример 2: Нужен план
```bash
python -m agents.cli plan -g "Добавить систему лояльности с бонусами"
```

### Пример 3: Нужна документация
```bash
python -m agents.cli execute -t "Создай документацию" --type documentation
```

### Пример 4: Код-ревью
```bash
python -m agents.cli review --file handlers/payment_handlers.py
```

---

## 💡 Где использовать?

**В терминале Cursor** (Ctrl+` или View → Terminal)

Просто откройте терминал и выполните команду. Всё!

---

## ❓ FAQ

**Q: Нужно ли открывать opencode.ai?**
A: НЕТ! Всё работает автоматически.

**Q: Нужно ли что-то настраивать?**
A: НЕТ! Всё уже настроено.

**Q: Как понять что работает?**
A: Запустите `python -m agents.cli agents` - увидите список агентов.

**Q: Что если команда не работает?**
A: Просто продолжайте работать со мной в чате (как обычно). Агенты - это дополнение, не обязательно.

---

## 🚀 Попробуйте сейчас

1. Откройте терминал (Ctrl+`)
2. Выполните: `python -m agents.cli agents`
3. Если видите список агентов - всё работает! ✅

**Готово! Теперь вы можете использовать агентов!** 🎉

