# 🤖 Интеграция AI (OpenCode) в SUPFLOT бот

## 📋 Обзор

В проект интегрирован AI сервис на базе OpenCode для расширения функциональности бота:
- **Автоматическая генерация описаний** для досок
- **Умная поддержка** - ответы на вопросы пользователей
- **Улучшенный парсинг дат** из естественного языка (опционально)

## 🚀 Установка OpenCode

OpenCode уже установлен в WSL окружении. Для использования нужно:

1. Убедиться, что OpenCode доступен:
   ```bash
   wsl bash -c "~/.opencode/bin/opencode --version"
   ```

2. Добавить в `.env` (опционально):
   ```env
   AI_ENABLED=true
   OPENCODE_PATH=  # Путь к opencode (если не в PATH)
   ```

## 🎯 Использование

### 1. Автоматическая генерация описаний досок

Когда партнер добавляет доску и отправляет "-" вместо описания, AI автоматически генерирует привлекательное описание на основе названия и цены.

**Где используется:** `handlers/partner_handlers.py` → `partner_board_description_input()`

### 2. Умная поддержка

Бот отвечает на свободные текстовые сообщения пользователей через AI, если вопрос не распознан как команда или кнопка меню.

**Где используется:** `handlers/user_handlers.py` → `handle_free_text()`

**Особенности:**
- Работает только когда пользователь не в FSM состоянии
- Использует контекст пользователя (количество активных бронирований)
- Если AI недоступен, отправляет стандартное сообщение с подсказками

### 3. Улучшенный парсинг дат

Добавлена функция `parse_date_natural_language()` в `utils/date_parser.py` для парсинга дат из естественного языка (например, "завтра", "послезавтра", "15 января").

**Примечание:** В текущей реализации используется кнопочный выбор дат, но функция доступна для будущего расширения.

## 📁 Структура файлов

- `services/ai_service.py` - Основной AI сервис для работы с OpenCode
- `config.py` - Добавлены настройки `AI_ENABLED` и `OPENCODE_PATH`
- `handlers/partner_handlers.py` - Интеграция генерации описаний
- `handlers/user_handlers.py` - Интеграция умной поддержки
- `utils/date_parser.py` - Функция улучшенного парсинга дат

## ⚙️ Конфигурация

В `config.py` добавлены:

```python
# AI / OpenCode
AI_ENABLED = os.getenv("AI_ENABLED", "false").lower() == "true"
OPENCODE_PATH = os.getenv("OPENCODE_PATH", "")  # Путь к opencode (если не в PATH)
```

## 🔧 Как работает AI сервис

1. **Проверка доступности:** При инициализации проверяется наличие OpenCode в системе
2. **Вызов через subprocess:** Используется асинхронный вызов через `asyncio.to_thread` и `subprocess.run`
3. **Временные файлы:** Промпты передаются через временные файлы для надежности
4. **Обработка ошибок:** Все ошибки логируются, сервис работает gracefully при недоступности AI

## 🎨 Примеры использования

### Генерация описания доски:

```python
from services.ai_service import get_ai_service

ai_service = get_ai_service()
description = await ai_service.generate_board_description(
    board_name="SUP доска Pro",
    price=800.0
)
```

### Ответ на вопрос пользователя:

```python
response = await ai_service.generate_support_response(
    user_message="Когда можно забронировать доску?",
    user_context={"bookings_count": 2}
)
```

### Парсинг даты:

```python
from utils.date_parser import parse_date_natural_language

date = await parse_date_natural_language("завтра")
```

## ⚠️ Важные замечания

1. **Производительность:** Вызовы AI могут занимать до 30 секунд (таймаут)
2. **Зависимость от WSL:** В Windows требуется WSL для работы OpenCode
3. **Опциональность:** Все функции AI работают gracefully при недоступности - бот продолжает работать без AI
4. **Логирование:** Все вызовы AI логируются для отладки

## 🔮 Будущие улучшения

- [ ] Кеширование ответов AI для часто задаваемых вопросов
- [ ] Интеграция с системой отзывов (анализ тональности)
- [ ] Автоматическое улучшение описаний существующих досок
- [ ] Генерация рекомендаций для пользователей
- [ ] Анализ фотографий досок для автоматического описания

## 📝 Примечания

Интеграция выполнена таким образом, что бот полностью функционален даже без AI. Все AI функции являются дополнительными и не критичны для основной функциональности.

