# 🚀 Полное руководство по деплою SUPFLOT

## 🎯 Варианты хостинга

### 1. **Railway.app** ⭐ (Рекомендуется)
- ✅ Бесплатный тариф ($5 кредитов/месяц)
- ✅ Автоматический деплой из GitHub
- ✅ PostgreSQL встроен
- ✅ Простая настройка

### 2. **Render.com**
- ✅ Бесплатный тариф
- ✅ Автоматический деплой
- ✅ PostgreSQL

### 3. **Fly.io**
- ✅ Бесплатный тариф
- ✅ Быстрый деплой
- ✅ Глобальная сеть

## 📋 Деплой на Railway.app (Пошагово)

### Шаг 1: Регистрация

1. Перейдите на https://railway.app
2. Нажмите "Login" → "Login with GitHub"
3. Авторизуйте доступ к репозиторию

### Шаг 2: Создание проекта

1. Нажмите "New Project"
2. Выберите "Deploy from GitHub repo"
3. Выберите репозиторий `tr1h/supflot`
4. Railway автоматически определит Python проект

### Шаг 3: Настройка бота

1. Railway создаст сервис автоматически
2. Перейдите в "Variables" и добавьте:
   ```
   BOT_TOKEN=7838017790:AAHZBCel15n0FJHwgZo6F5IQ4Lqt3k2KWdE
   DB_NAME=supbot.db
   ADMIN_IDS=202140267,1383730017
   PLATFORM_COMMISSION_PERCENT=10
   WORK_HOURS_START=8
   WORK_HOURS_END=22
   ```
3. В "Settings" → "Start Command" укажите: `python run_bot.py`

### Шаг 4: Настройка сайта

1. Нажмите "+ New" → "GitHub Repo"
2. Выберите тот же репозиторий `tr1h/supflot`
3. В "Settings" → "Root Directory" укажите: `orders_site`
4. В "Start Command" укажите: `python app.py`
5. Добавьте переменные:
   ```
   PORT=5000
   DB_NAME=SupBot.db
   YK_SHOP_ID=ваш_shop_id
   YK_SECRET=ваш_secret
   ```

### Шаг 5: База данных (опционально)

1. Нажмите "+ New" → "Database" → "PostgreSQL"
2. Railway создаст БД автоматически
3. Получите строку подключения из "Variables"
4. Обновите `DB_NAME` на строку подключения PostgreSQL

### Шаг 6: Домен

1. В настройках сервиса сайта → "Settings" → "Generate Domain"
2. Или подключите свой домен `supflot.pro`

## 📋 Деплой на Render.com

### Шаг 1: Регистрация

1. Перейдите на https://render.com
2. Войдите через GitHub

### Шаг 2: Создание Web Service для бота

1. Нажмите "New" → "Web Service"
2. Подключите репозиторий `tr1h/supflot`
3. Настройки:
   - **Name**: `supflot-bot`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python run_bot.py`
4. Добавьте переменные окружения (Environment Variables)

### Шаг 3: Создание Web Service для сайта

1. Нажмите "New" → "Web Service"
2. Подключите тот же репозиторий
3. Настройки:
   - **Name**: `supflot-website`
   - **Root Directory**: `orders_site`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python app.py`
4. Добавьте переменные окружения

## 🔧 Настройка переменных окружения

### Для бота:
```
BOT_TOKEN=7838017790:AAHZBCel15n0FJHwgZo6F5IQ4Lqt3k2KWdE
DB_NAME=supbot.db
ADMIN_IDS=202140267,1383730017
PLATFORM_COMMISSION_PERCENT=10
WORK_HOURS_START=8
WORK_HOURS_END=22
REVIEW_CHANNEL=@supflot_reviews
```

### Для сайта:
```
PORT=5000
DB_NAME=SupBot.db
YK_SHOP_ID=ваш_shop_id
YK_SECRET=ваш_secret
OPENWEATHER_KEY=ваш_ключ (опционально)
```

## 🌐 Настройка домена supflot.pro

### Для Railway:
1. В настройках сервиса → "Settings" → "Custom Domain"
2. Добавьте `supflot.pro`
3. Настройте DNS записи:
   - Тип: `CNAME`
   - Имя: `@` или `www`
   - Значение: `ваш-проект.up.railway.app`

### Для Render:
1. В настройках сервиса → "Custom Domains"
2. Добавьте `supflot.pro`
3. Настройте DNS записи по инструкции Render

## ✅ Проверка работы

### Бот:
1. Найдите бота в Telegram
2. Отправьте `/start`
3. Проверьте ответ

### Сайт:
1. Откройте URL из Railway/Render
2. Проверьте главную страницу
3. Проверьте API: `https://ваш-домен/locations`

## 🐛 Решение проблем

### Бот не отвечает:
- Проверьте логи в Railway/Render
- Проверьте переменную `BOT_TOKEN`
- Убедитесь, что бот запущен

### Сайт не открывается:
- Проверьте переменную `PORT`
- Проверьте логи
- Убедитесь, что сайт использует `0.0.0.0` как host

### Ошибки базы данных:
- SQLite работает на хостинге, но данные теряются при перезапуске
- Используйте PostgreSQL для продакшена

## 📊 Мониторинг

- Railway: встроенные логи и метрики
- Render: логи в реальном времени
- Настройте уведомления о падении сервисов

## 🔄 Автоматический деплой

Оба сервиса автоматически деплоят при push в `main` ветку!

Просто делайте:
```bash
git add .
git commit -m "Update"
git push
```

И Railway/Render автоматически обновят сервисы!

