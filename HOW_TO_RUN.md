# 🚀 Как запустить SUPFLOT

## ✅ У вас есть:

1. **Telegram Бот** (`run_bot.py`) - полностью рабочий
2. **Веб-сайт** (`orders_site/app.py`) - полностью рабочий

## 📋 Шаги для запуска:

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Создайте файл `.env`

Скопируйте `.env.example` в `.env` и заполните:

```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

Затем откройте `.env` и заполните:
- `BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather)
- `YK_SHOP_ID` и `YK_SECRET` - для платежей (если используете)
- Остальное опционально

### 3. Запустите Telegram бота

```bash
python run_bot.py
```

Бот начнет работать и будет отвечать на команды в Telegram!

### 4. Запустите веб-сайт (в отдельном терминале)

```bash
cd orders_site
python app.py
```

Сайт будет доступен по адресу: http://localhost:5000

## 🎯 Что работает:

### Telegram Бот:
- ✅ `/start` - начало работы
- ✅ Бронирование сапбордов
- ✅ Партнерская система
- ✅ Платежи
- ✅ Админ-панель
- ✅ Кошелек партнеров

### Веб-сайт:
- ✅ Главная страница с бронированием
- ✅ API для бронирований
- ✅ Интеграция с ЮKassa
- ✅ Погода
- ✅ Календарь доступности

## ⚠️ Важно:

1. **Бот и сайт используют разные базы данных**:
   - Бот: `supbot.db` (из `core/database.py`)
   - Сайт: `SupBot.db` (из `orders_site/app.py`)
   
   В будущем нужно объединить!

2. **Для продакшена**:
   - Используйте PostgreSQL вместо SQLite
   - Настройте веб-сервер (Nginx + Gunicorn)
   - Настройте SSL сертификат
   - Используйте supervisor для бота

## 🐛 Если что-то не работает:

1. Проверьте `.env` файл
2. Проверьте, что все зависимости установлены
3. Проверьте логи в консоли
4. Убедитесь, что порт 5000 свободен (для сайта)

