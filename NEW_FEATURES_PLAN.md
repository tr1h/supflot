# 📋 План: Добавление новых функций

## 🎯 Цель
Добавить три новые функции: Суточная аренда, Мультибронь, Каталог локаций и досок.

---

## 🏗️ Функция 1: Суточная аренда

### Описание
Аренда доски на полные сутки (24 часа) с фиксированной ценой.

### Этапы реализации:

#### 1.1 База данных
- ✅ Таблица `bookings` уже поддерживает длительность
- ⚠️ Возможно, нужно добавить поле `booking_type` для различения типов
- Или использовать специальную длительность (1440 минут = 24 часа)

#### 1.2 Обновление BookingService
**Файл:** `services/booking_service.py`

**Изменения:**
- Метод `check_board_availability` уже работает с длительностью
- Добавить метод `check_daily_rental_availability()` - проверка доступности на сутки
- Расчет стоимости для суточной аренды (может быть фиксированная цена или цена × 24)

#### 1.3 Обработчики
**Файл:** `handlers/booking_handlers.py`

**Новая логика:**
- Обработчик `booking_type:daily` - начало суточной аренды
- Пропустить выбор времени (время начала = 00:00 или выбранное пользователем)
- Длительность фиксированная = 1440 минут (24 часа)
- Расчет стоимости: `price_per_day` или `price_per_hour × 24`

**Новые состояния (если нужно):**
- `choosing_daily_date` - выбор даты для суточной аренды
- `choosing_daily_start_time` - выбор времени начала (опционально)

#### 1.4 Клавиатуры
**Файл:** `keyboards/user.py`

- Обновить `get_booking_type_keyboard()` - кнопка уже есть
- Добавить клавиатуру для выбора даты суточной аренды (можно использовать существующую)

#### 1.5 Схема бронирования
```
1. Выбор типа: "Суточная аренда"
2. Выбор локации
3. Выбор доски
4. Выбор даты начала
5. (Опционально) Выбор времени начала
6. Расчет стоимости (фиксированная или ×24)
7. Выбор способа оплаты
8. Создание бронирования
```

---

## 🎯 Функция 2: Мультибронь

### Описание
Бронирование нескольких разных досок одновременно на одно и то же время.

### Этапы реализации:

#### 2.1 База данных
- ✅ Таблица `bookings` уже поддерживает `quantity` (количество одной доски)
- ⚠️ Для мультиброни нужна новая таблица или структура:
  
**Вариант 1: Новая таблица `multi_bookings`**
```sql
CREATE TABLE multi_bookings (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    booking_date DATE,
    start_time INTEGER,
    start_minute INTEGER,
    duration INTEGER,
    status TEXT,
    payment_method TEXT,
    total_amount REAL,
    created_at TIMESTAMP
);

CREATE TABLE multi_booking_items (
    id INTEGER PRIMARY KEY,
    multi_booking_id INTEGER,
    board_id INTEGER,
    quantity INTEGER,
    amount REAL,
    FOREIGN KEY (multi_booking_id) REFERENCES multi_bookings(id),
    FOREIGN KEY (board_id) REFERENCES boards(id)
);
```

**Вариант 2: Использовать существующую таблицу с группировкой**
- Добавить поле `group_id` в таблицу `bookings`
- Группировать бронирования по `group_id`

#### 2.2 Сервис
**Файл:** `services/booking_service.py` (или новый `multi_booking_service.py`)

**Методы:**
- `create_multi_booking()` - создание мультиброни
- `check_multi_booking_availability()` - проверка доступности всех досок
- `get_multi_booking()` - получение мультиброни
- `update_multi_booking_status()` - обновление статуса

#### 2.3 Обработчики
**Файл:** `handlers/booking_handlers.py` (или новый `multi_booking_handlers.py`)

**Новые состояния:**
- `choosing_boards_for_multi` - выбор досок для мультиброни
- `choosing_multi_quantities` - выбор количества для каждой доски
- `confirming_multi_booking` - подтверждение мультиброни

**Логика:**
1. Выбор типа: "Мультибронь"
2. Выбор локации (или всех локаций)
3. Выбор нескольких досок (с чекбоксами)
4. Выбор даты и времени (одинаковое для всех)
5. Выбор количества для каждой доски
6. Проверка доступности всех досок
7. Расчет общей стоимости
8. Подтверждение
9. Создание бронирований (отдельное для каждой доски или групповое)
10. Оплата

#### 2.4 UI/UX
- Клавиатура с возможностью выбора нескольких досок
- Подтверждающий экран со списком выбранных досок и суммами
- Отображение общей стоимости

---

## 📚 Функция 3: Каталог локаций и досок

### Описание
Просмотр всех доступных локаций и досок без начала бронирования. Поиск, фильтрация, просмотр деталей.

### Этапы реализации:

#### 3.1 Обработчики
**Файл:** `handlers/catalog_handlers.py` (новый файл)

**Функционал:**
- Просмотр всех локаций
- Просмотр досок в локации
- Просмотр деталей доски (без бронирования)
- Поиск по названию
- Фильтрация по цене, рейтингу (если есть)

**Обработчики:**
- `catalog_locations` - список локаций
- `catalog_location_boards` - доски в локации
- `catalog_board_detail` - детали доски
- `catalog_search` - поиск
- `catalog_filters` - фильтры

#### 3.2 Клавиатуры
**Файл:** `keyboards/user.py` (добавить)

**Новые клавиатуры:**
- `get_catalog_menu_keyboard()` - меню каталога
- `get_catalog_locations_keyboard()` - список локаций
- `get_catalog_boards_keyboard()` - список досок
- `get_catalog_filters_keyboard()` - фильтры

#### 3.3 Сервис (опционально)
**Файл:** `services/catalog_service.py` (новый файл)

**Методы:**
- `get_all_locations()` - все локации
- `get_location_boards()` - доски в локации
- `search_boards()` - поиск досок
- `filter_boards()` - фильтрация досок

#### 3.4 Интеграция
**Файл:** `handlers/user_handlers.py`

- Обработчик кнопки "📚 Каталог" (уже есть в меню)
- Регистрация обработчиков каталога

**Файл:** `run_bot.py`

- Зарегистрировать `register_catalog_handlers()`

#### 3.5 Схема навигации
```
Каталог (главное меню)
  ├─ Все локации
  │   └─ Доски в локации
  │       └─ Детали доски
  │           └─ [Кнопка "Забронировать"]
  ├─ Поиск
  └─ Фильтры
```

---

## 📊 Приоритеты реализации

### Приоритет 1: Каталог (самый простой)
**Время:** 30-45 минут  
**Сложность:** Низкая  
**Польза:** Высокая - пользователи смогут просматривать доступные доски

### Приоритет 2: Суточная аренда
**Время:** 45-60 минут  
**Сложность:** Средняя  
**Польза:** Средняя - дополнительный тип аренды

### Приоритет 3: Мультибронь
**Время:** 2-3 часа  
**Сложность:** Высокая  
**Польза:** Средняя - сложная логика, требуется новая структура БД

---

## 🔄 Порядок реализации

### Рекомендуемый порядок:

1. **Каталог локаций и досок** (начать с этого - проще всего)
   - Создать `handlers/catalog_handlers.py`
   - Добавить клавиатуры
   - Интегрировать в меню
   - Протестировать

2. **Суточная аренда**
   - Обновить обработчики бронирования
   - Добавить логику для суточной аренды
   - Протестировать

3. **Мультибронь**
   - Решить структуру БД (новая таблица или группировка)
   - Создать сервис для мультиброни
   - Создать обработчики
   - Протестировать

---

## 💡 Дополнительные улучшения (опционально)

1. **Для суточной аренды:**
   - Специальные цены для суточной аренды в таблице `boards` (поле `daily_price`)
   - Скидки за суточную аренду

2. **Для мультиброни:**
   - Скидка при бронировании нескольких досок
   - Возможность выбрать разные даты/времена для разных досок

3. **Для каталога:**
   - Просмотр фото досок
   - Отображение рейтинга (если есть отзывы)
   - Сортировка (по цене, рейтингу, названию)
   - Избранное (сохранение досок)

---

## 📝 Заметки

- Все три функции используют существующую инфраструктуру (BookingService, платежи, уведомления)
- Каталог не требует изменений в БД
- Суточная аренда требует минимальных изменений (только логика)
- Мультибронь требует серьезных изменений в БД или создания новой структуры

---

**План готов к реализации!** 🚀

