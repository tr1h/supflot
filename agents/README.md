# 🤖 Система агентов OpenCode для SUPFLOT

Система агентов для автоматизации разработки через OpenCode, аналогичная подходу Zukutoke.

## 📋 Структура

Система состоит из:

1. **Оркестратор** (`orchestrator.py`) - управляет всеми агентами и распределяет задачи
2. **Агенты** - специализированные агенты для различных задач:
   - **PlanningAgent** - планирование разработки
   - **DevelopmentAgent** - написание кода
   - **DocumentationAgent** - создание документации
   - **TestingAgent** - написание тестов и поиск багов

## 🚀 Использование

### Через CLI

#### Выполнение задачи (автоматический выбор агента):

```bash
python -m agents.cli execute -t "Реализуй функцию для парсинга дат из естественного языка"
```

#### Выполнение через конкретного агента:

```bash
# Планирование
python -m agents.cli execute -t "Создай план разработки системы отзывов" --type planning

# Разработка
python -m agents.cli execute -t "Реализуй функцию отправки уведомлений" --type development

# Документация
python -m agents.cli execute -t "Документируй класс BookingService" --type documentation

# Тестирование
python -m agents.cli execute -t "Напиши тесты для handlers/booking_handlers.py" --type testing
```

#### Планирование и выполнение:

```bash
python -m agents.cli plan -g "Добавить систему отзывов в бот"
```

#### Код-ревью:

```bash
# Ревью файла
python -m agents.cli review --file handlers/user_handlers.py --focus security

# Ревью кода из строки
python -m agents.cli review --code "def test(): pass" --focus performance
```

#### Пайплайн задач:

Создайте файл `pipeline.json`:
```json
[
  {
    "task": "Создай план реализации функции X",
    "type": "planning",
    "required": true
  },
  {
    "task": "Реализуй функцию X согласно плану",
    "type": "development",
    "context": {"plan": "{{step_0_result}}"},
    "required": true
  },
  {
    "task": "Напиши тесты для функции X",
    "type": "testing",
    "required": false
  }
]
```

Запуск:
```bash
python -m agents.cli pipeline --file pipeline.json
```

#### Информация об агентах:

```bash
python -m agents.cli agents
```

### Через Python код

```python
import asyncio
from agents.orchestrator import get_orchestrator

async def main():
    orchestrator = get_orchestrator()
    
    # Выполнение задачи
    result = await orchestrator.execute_task(
        task="Реализуй функцию для парсинга дат",
        task_type="development"
    )
    
    if result["success"]:
        print(result["result"])
    else:
        print(f"Ошибка: {result['error']}")

asyncio.run(main())
```

### Использование конкретных агентов

```python
from agents.planning_agent import PlanningAgent
from agents.development_agent import DevelopmentAgent

async def example():
    # Планирование
    planner = PlanningAgent()
    plan = await planner.create_roadmap([
        "Система отзывов",
        "Управление сотрудниками",
        "Календарь бронирований"
    ])
    
    # Разработка
    developer = DevelopmentAgent()
    code = await developer.implement_feature(
        "Функция для отправки уведомлений пользователям",
        context={"existing_code": "..."}
    )
```

## 🎯 Типы задач

Оркестратор автоматически определяет тип задачи по ключевым словам:

- **planning**: план, планирование, roadmap, задачи, приоритет
- **testing**: тест, проверка, баг, ошибка, исправить
- **documentation**: документ, readme, описать, комментарий
- **development**: всё остальное (по умолчанию)

## 📝 Примеры использования

### Планирование новой функции

```bash
python -m agents.cli execute -t "Создай детальный план для реализации системы отзывов: пользователи могут оставлять отзывы после завершенного бронирования, партнеры могут видеть отзывы к своим локациям" --type planning
```

### Реализация функции

```bash
python -m agents.cli execute -t "Реализуй функцию create_review(user_id, booking_id, rating, comment) которая создает отзыв в базе данных" --type development
```

### Написание документации

```bash
python -m agents.cli execute -t "Создай документацию для модуля agents/ - опиши структуру, использование, примеры" --type documentation
```

### Написание тестов

```bash
python -m agents.cli execute -t "Напиши pytest тесты для функции parse_date из utils/date_parser.py, покрой основные и граничные случаи" --type testing
```

### Код-ревью

```bash
python -m agents.cli review --file handlers/payment_handlers.py --focus security
```

## 🔧 Конфигурация

Система использует настройки из `config.py`:
- `AI_ENABLED` - включение/выключение AI
- `OPENCODE_PATH` - путь к OpenCode (если не в PATH)

Убедитесь, что OpenCode установлен и доступен:
```bash
wsl bash -c "~/.opencode/bin/opencode --version"
```

## 💡 Рекомендации

1. **Используйте планирование** для сложных задач - сначала создайте план, затем реализуйте
2. **Код-ревью** перед коммитом - используйте `review` для проверки кода
3. **Документируйте** новый код сразу - используйте `documentation` агента
4. **Пишите тесты** - используйте `testing` агента для генерации тестов
5. **Пайплайны** для сложных задач - используйте pipeline для последовательного выполнения

## 🎨 Интеграция с Cursor

Эта система работает как дополнение к Cursor:
- Используйте Cursor для интерактивной разработки
- Используйте агентов для автоматизации повторяющихся задач
- Используйте оркестратор для сложных многоэтапных задач

## 📚 Дополнительные возможности

### Кастомные агенты

Вы можете создать собственных агентов, наследуясь от `BaseAgent`:

```python
from agents.base_agent import BaseAgent

class CustomAgent(BaseAgent):
    def __init__(self):
        super().__init__(
            name="CustomAgent",
            role="Кастомная роль",
            system_prompt="Твой системный промпт"
        )
    
    def _get_task_instructions(self) -> str:
        return "Инструкции для задач"
```

### Расширение оркестратора

Добавьте ваших агентов в оркестратор:

```python
from agents.orchestrator import Orchestrator

orchestrator = Orchestrator()
orchestrator.agents["custom"] = CustomAgent()
```

## ⚠️ Примечания

- Все агенты используют OpenCode через `AIService`
- Если OpenCode недоступен, агенты будут работать gracefully (возвращать ошибки)
- Результаты могут варьироваться в зависимости от настроек OpenCode и используемой модели
- Рекомендуется проверять и дорабатывать код, сгенерированный агентами

