# 🚀 Деплой SUPFLOT на Render.com

## ✅ Почему Render.com?

- ✅ **Полностью бесплатно** (с ограничениями)
- ✅ Автоматический деплой из GitHub
- ✅ PostgreSQL доступен
- ✅ Простая настройка
- ✅ Поддержка Python
- ✅ Бесплатный SSL сертификат

## 📋 Пошаговая инструкция

### Шаг 1: Регистрация на Render

1. Перейдите на https://render.com
2. Нажмите "Get Started for Free"
3. Войдите через GitHub
4. Авторизуйте доступ к репозиторию `tr1h/supflot`

### Шаг 2: Деплой бота (Worker Service)

1. В Dashboard нажмите **"New +"** → **"Background Worker"**
2. Подключите репозиторий:
   - **Repository**: `tr1h/supflot`
   - **Branch**: `main`
3. Настройки:
   - **Name**: `supflot-bot`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python run_bot.py`
   - **Plan**: `Free` (или `Starter` за $7/мес без сна)
4. Добавьте переменные окружения (Environment Variables):
   ```
   BOT_TOKEN=7838017790:AAHZBCel15n0FJHwgZo6F5IQ4Lqt3k2KWdE
   DB_NAME=supbot.db
   ADMIN_IDS=202140267,1383730017
   PLATFORM_COMMISSION_PERCENT=10
   WORK_HOURS_START=8
   WORK_HOURS_END=22
   REVIEW_CHANNEL=@supflot_reviews
   ```
5. Нажмите **"Create Background Worker"**

### Шаг 3: Деплой сайта (Web Service)

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите репозиторий:
   - **Repository**: `tr1h/supflot`
   - **Branch**: `main`
3. Настройки:
   - **Name**: `supflot-website`
   - **Environment**: `Python 3`
   - **Root Directory**: `orders_site` ⚠️ ВАЖНО!
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python app.py`
   - **Plan**: `Free` (или `Starter` за $7/мес без сна)
4. Добавьте переменные окружения:
   ```
   PORT=5000
   HOST=0.0.0.0
   DB_NAME=SupBot.db
   YK_SHOP_ID=ваш_shop_id (если есть)
   YK_SECRET=ваш_secret (если есть)
   OPENWEATHER_KEY=ваш_ключ (опционально)
   ```
5. Нажмите **"Create Web Service"**

### Шаг 4: Настройка домена supflot.pro

1. В настройках Web Service → **"Settings"** → **"Custom Domains"**
2. Нажмите **"Add Custom Domain"**
3. Введите: `supflot.pro`
4. Render покажет DNS записи для настройки

#### Настройка DNS в reg.ru:

**Вариант 1: CNAME (если поддерживается для @)**
```
Тип: CNAME
Имя: @
Значение: supflot-website.onrender.com
TTL: 3600
```

**Вариант 2: A запись (если CNAME не поддерживается)**
```
Тип: A
Имя: @
Значение: [IP адрес от Render]
TTL: 3600
```

**Для www:**
```
Тип: CNAME
Имя: www
Значение: supflot-website.onrender.com
TTL: 3600
```

5. Настройте DNS записи в панели reg.ru:
   - Войдите в reg.ru
   - Домены → supflot.pro → DNS-серверы и зона
   - Добавьте записи выше
6. Подождите 5-30 минут для активации
7. SSL сертификат установится автоматически

📖 **Подробная инструкция**: см. `DNS_SETUP.md`

### Шаг 5: Настройка базы данных (опционально)

Для продакшена лучше использовать PostgreSQL:

1. Нажмите **"New +"** → **"PostgreSQL"**
2. Настройки:
   - **Name**: `supflot-db`
   - **Database**: `supflot`
   - **User**: `supflot_user`
   - **Plan**: `Free` (или `Starter` за $7/мес)
3. После создания получите строку подключения:
   - Перейдите в настройки БД
   - Скопируйте **"Internal Database URL"**
4. Обновите переменные окружения в боте и сайте:
   ```
   DATABASE_URL=postgresql://user:pass@host:5432/dbname
   ```

## ⚠️ Важные моменты

### Бесплатный тариф (Free Plan):

- ✅ Сервисы работают бесплатно
- ⚠️ **"Засыпают" после 15 минут бездействия**
- ⚠️ Первый запрос после "сна" медленный (холодный старт ~30 сек)
- ⚠️ Ограничение: 750 часов/месяц

### Платный тариф (Starter - $7/мес):

- ✅ Сервисы не засыпают
- ✅ Быстрый старт всегда
- ✅ Больше ресурсов
- ✅ Приоритетная поддержка

**Рекомендация**: Начните с Free, при росте трафика переходите на Starter.

## 🔍 Проверка работы

### Бот:
1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Проверьте ответ

### Сайт:
1. Откройте URL: `https://supflot-website.onrender.com`
2. Или ваш домен: `https://supflot.pro` (после настройки DNS)
3. Проверьте главную страницу
4. Проверьте API: `/locations`

## 📊 Мониторинг

1. В Dashboard Render видно:
   - Логи в реальном времени
   - Использование ресурсов
   - Статус сервисов
   - Метрики производительности

2. Настройте уведомления:
   - Settings → Notifications
   - Email при падении сервисов

## 🔄 Автоматический деплой

Render автоматически деплоит при каждом push в `main` ветку!

Просто делайте:
```bash
git add .
git commit -m "Update"
git push
```

И Render автоматически обновит сервисы через 1-2 минуты!

## 🐛 Решение проблем

### Бот не отвечает:
1. Проверьте логи в Render Dashboard
2. Убедитесь, что `BOT_TOKEN` правильный
3. Проверьте, что Worker запущен (зеленый статус)

### Сайт не открывается:
1. Проверьте логи
2. Убедитесь, что `PORT=5000` установлен
3. Проверьте, что `HOST=0.0.0.0`
4. Убедитесь, что Root Directory = `orders_site`

### Домен не работает:
1. Проверьте DNS записи в reg.ru
2. Подождите 30 минут для обновления DNS
3. Проверьте настройки Custom Domain в Render
4. Убедитесь, что SSL сертификат установлен

### Ошибки базы данных:
1. SQLite работает, но данные теряются при перезапуске
2. Используйте PostgreSQL для продакшена
3. Обновите код для работы с PostgreSQL

### Холодный старт (медленный первый запрос):
- Это нормально для Free плана
- Переходите на Starter ($7/мес) для устранения

## 📝 Чеклист деплоя

- [ ] Зарегистрирован на Render.com
- [ ] Создан Background Worker для бота
- [ ] Добавлены все переменные окружения для бота
- [ ] Создан Web Service для сайта
- [ ] Указан Root Directory = `orders_site`
- [ ] Добавлены все переменные окружения для сайта
- [ ] Настроен Custom Domain `supflot.pro`
- [ ] Настроены DNS записи в reg.ru
- [ ] Бот отвечает в Telegram
- [ ] Сайт открывается по домену
- [ ] SSL сертификат работает
- [ ] Настроен PostgreSQL (опционально)

## 🎉 Готово!

После выполнения всех шагов:
- ✅ Бот работает 24/7 на Render
- ✅ Сайт доступен по `https://supflot.pro`
- ✅ Автоматический деплой из GitHub
- ✅ Бесплатный SSL сертификат

**Время деплоя: ~10-15 минут** ⚡
