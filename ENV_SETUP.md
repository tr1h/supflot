# 🔧 Настройка .env файла

## ✅ Обязательные параметры (нужны для запуска)

### 1. BOT_TOKEN
Токен вашего Telegram бота от @BotFather

**Как получить:**
1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и придумайте имя для бота
4. Скопируйте полученный токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

**В .env:**
```
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 2. ADMIN_IDS
Ваш Telegram ID (ID администратора)

**Как узнать свой ID:**
1. Напишите боту @userinfobot
2. Он покажет ваш ID (например, `123456789`)
3. Если несколько админов, перечислите через запятую: `123456789,987654321`

**В .env:**
```
ADMIN_IDS=123456789
```

## ⚙️ Опциональные параметры (можно настроить позже)

### Рабочие часы
```
WORK_HOURS_START=8     # Начало работы (8:00)
WORK_HOURS_END=22      # Конец работы (22:00)
```

### База данных
```
DB_NAME=supbot.db      # Имя файла БД (по умолчанию)
```

### Платежи (если планируете использовать)
```
# Telegram Payments (от BotFather)
PAYMENTS_PROVIDER_TOKEN=your_payment_provider_token_here

# Детали карты для ручных платежей
PAYMENT_CARD_DETAILS=1234 5678 9012 3456

# YooKassa (опционально)
YK_SHOP_ID=your_shop_id
YK_SECRET=your_secret_key
```

### API ключи (опционально)
```
# OpenWeatherMap (для прогноза погоды)
OPENWEATHER_KEY=your_api_key_here

# Yandex Maps (для карт в Mini App)
YANDEX_MAPS_API_KEY=your_api_key_here
```

### Другие настройки
```
# Комиссия платформы (%)
PLATFORM_COMMISSION_PERCENT=10.0

# URL Mini App (если используете)
MINIAPP_URL=https://your-domain.com/miniapp

# Режим разработки
DEV_MODE=false

# Flask секретный ключ
FLASK_SECRET_KEY=dev-secret-key-change-in-production
```

## 📝 Пример минимального .env файла

```env
# Обязательные
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_IDS=123456789

# Остальное можно оставить по умолчанию
DB_NAME=supbot.db
WORK_HOURS_START=8
WORK_HOURS_END=22
PLATFORM_COMMISSION_PERCENT=10.0
DEV_MODE=false
```

## 🚀 После настройки

1. Сохраните файл `.env`
2. Запустите бота: `python run_bot.py`
3. Бот должен успешно запуститься!

## ❓ Проблемы?

- Если бот не запускается, проверьте логи в `bot.log`
- Убедитесь, что токен бота правильный
- Проверьте, что ADMIN_IDS содержит только цифры



