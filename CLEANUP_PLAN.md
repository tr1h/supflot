# 🧹 План очистки проекта SUPFLOT

## 📋 Категории файлов для удаления

### 1. 🗑️ Мусорные/временные файлы (УДАЛИТЬ)

- `=` - пустой файл
- `cd` - пустой файл  
- `conn.cursor()` - пустой файл
- `bot.log` - лог файл (будет создаваться заново)
- `supbot.sqbpro` - файл SQLite Browser (не нужен в репозитории)

### 2. 🗄️ Базы данных (УДАЛИТЬ локально, уже в .gitignore)

- `SupBot.db`, `SupBot.db-shm`, `SupBot.db-wal`
- `core/supbot.db`
- `core/sup_ultimate.db`
- `supclub.db`
- `your_db.sqlite3`
- `orders_site/db.sqlite`

**Примечание:** Базы данных уже в `.gitignore`, но можно удалить локально для экономии места.

### 3. 📝 Старые/дублирующиеся скрипты (ПРОВЕРИТЬ и УДАЛИТЬ)

- `main.py` - старый файл бота (используется `run_bot.py`)
  - ⚠️ Используется в некоторых handlers, но основной - `run_bot.py`
  
- `database.py` в корне - возможно дубликат `core/database.py`
  - ⚠️ Используется в `main.py` и `orders_site/auth.py`
  
- `test.py` - тестовый скрипт для миграции (можно удалить)
- `create_table.py` - старый скрипт создания таблиц (не нужен)
- `update_order_status.py` - устаревший скрипт с хардкодом пути

### 4. 🌐 Старые веб-проекты (ПРОВЕРИТЬ)

- `webapp/` - Django приложение (старый проект?)
  - Проверить, используется ли
  
- `web_admin.py` - старый админ на Bottle (не используется)
  - Используется `orders_site/` вместо этого

### 5. 🔧 Утилиты (ОСТАВИТЬ или ПЕРЕМЕСТИТЬ)

- `ngrok.exe` - полезно для разработки, но лучше в `.gitignore`
- `seed_test_data.py` - полезно для разработки, оставить

### 6. 🐛 Файлы с ошибками в именах (ИСПРАВИТЬ)

- `orders_site/auth/views.py .py` - файл с пробелом в имени
  - Переименовать в `views.py`

### 7. 📦 Старые handlers (ПРОВЕРИТЬ)

Файлы с префиксом `NEW_` - возможно старые версии:
- Проверить, используются ли они в `run_bot.py`
- Если нет - можно удалить или переместить в архив

## ✅ Рекомендации

### Безопасная очистка (можно удалить сразу):

1. Мусорные файлы (`=`, `cd`, `conn.cursor()`)
2. Логи (`bot.log`)
3. Файлы SQLite Browser (`.sqbpro`)
4. Тестовые скрипты (`test.py`, `create_table.py`, `update_order_status.py`)
5. Старый веб-админ (`web_admin.py`)

### Требует проверки:

1. `main.py` - проверить зависимости
2. `database.py` в корне - проверить использование
3. `webapp/` - проверить, нужен ли
4. Handlers с префиксом `NEW_` - проверить использование

### Оставить:

1. `run_bot.py` - основной файл бота
2. `orders_site/` - текущий веб-сайт
3. `core/` - ядро системы
4. `handlers/` - обработчики (после проверки)
5. Документация (`.md` файлы)

