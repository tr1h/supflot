# 🤖 Как использовать агентов - ПРОСТОЕ РУКОВОДСТВО

## ❓ Что такое OpenCode и наши агенты?

**OpenCode** - это инструмент, который мы **уже установили** и **уже используем**.
Вам **НЕ нужно** ничего делать с OpenCode отдельно!

**Наши агенты** - это обертка над OpenCode, которую мы создали специально для вашего проекта.

---

## 🎯 Как использовать агентов (3 простых шага)

### Вариант 1: Через наш CLI (рекомендуется)

Откройте терминал в Cursor и выполните:

```bash
# Показать список агентов
python -m agents.cli agents

# Выполнить задачу (агент выберется автоматически)
python -m agents.cli execute -t "Реализуй функцию для расчета суммы бронирования"

# Планирование
python -m agents.cli plan -g "Добавить систему отзывов"

# Код-ревью
python -m agents.cli review --file handlers/user_handlers.py
```

### Вариант 2: Через простой скрипт

```bash
# Быстрое выполнение задачи
python quick_agent.py "Создай план для функции X"

# Планирование
python quick_agent.py plan "Добавить новую функцию"
```

---

## 💡 Что происходит под капотом?

```
Вы запускаете команду
    ↓
Наши агенты получают задачу
    ↓
Агенты автоматически вызывают OpenCode (через WSL)
    ↓
OpenCode обрабатывает задачу через AI
    ↓
Результат возвращается вам
```

**Вам не нужно ничего настраивать!** Всё работает автоматически.

---

## 📝 Простые примеры

### Пример 1: Планирование

**Вы в терминале:**
```bash
python -m agents.cli plan -g "Добавить функцию отправки уведомлений"
```

**Что произойдет:**
- Агент планирования создаст план
- План будет показан в терминале
- Вы можете использовать план для разработки

### Пример 2: Разработка

**Вы в терминале:**
```bash
python -m agents.cli execute -t "Реализуй функцию для проверки доступности доски на дату"
```

**Что произойдет:**
- Агент разработки создаст код
- Код будет показан в терминале
- Вы можете скопировать и использовать

### Пример 3: Документация

**Вы в терминале:**
```bash
python -m agents.cli execute -t "Создай документацию для services/booking_service.py" --type documentation
```

**Что произойдет:**
- Агент документации создаст документацию
- Документация будет показана в терминале
- Вы можете сохранить в файл

---

## ❌ Что НЕ нужно делать

- ❌ НЕ нужно открывать сайт opencode.ai
- ❌ НЕ нужно регистрироваться где-то
- ❌ НЕ нужно запускать OpenCode отдельно
- ❌ НЕ нужно ничего настраивать дополнительно

**Всё уже настроено и работает!**

---

## ✅ Что нужно делать

1. **Откройте терминал** в Cursor (Ctrl+` или Terminal → New Terminal)
2. **Выполните команду** агента
3. **Получите результат**
4. **Используйте результат** в своем коде

Всё!

---

## 🎨 Реальный пример использования

Допустим, вы хотите добавить новую функцию. Что делать:

### Шаг 1: Планирование (опционально)
```bash
python -m agents.cli plan -g "Добавить функцию расчета скидки для постоянных клиентов"
```
→ Получаете план

### Шаг 2: Разработка
Вы можете:
- **Вариант А:** Попросить меня в чате Cursor (как обычно) - "Реализуй функцию расчета скидки"
- **Вариант Б:** Использовать агента:
  ```bash
  python -m agents.cli execute -t "Реализуй функцию расчета скидки для постоянных клиентов"
  ```

### Шаг 3: Документация (опционально)
```bash
python -m agents.cli execute -t "Создай документацию для функции расчета скидки" --type documentation
```

---

## 🚀 Быстрый старт

**Попробуйте прямо сейчас:**

1. Откройте терминал в Cursor
2. Выполните:
   ```bash
   python -m agents.cli agents
   ```
3. Вы увидите список доступных агентов

**Всё работает!** 🎉

---

## 💬 Резюме

- ✅ **OpenCode** - уже установлен и работает (через WSL)
- ✅ **Наши агенты** - готовы к использованию
- ✅ **Использование** - просто запускайте команды в терминале
- ✅ **Ничего дополнительно** настраивать не нужно

**Просто используйте команды агентов, всё остальное работает автоматически!**

